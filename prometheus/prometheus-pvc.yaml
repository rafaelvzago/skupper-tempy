apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-longhorn-pvc
  namespace: skupper-pi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi

